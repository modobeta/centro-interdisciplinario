import { useState } from 'react';import Button from '../../../components/ui/Button';import { sendMessage } from '../api/messagesApi'
export default function MessageComposer({conversationId,onSent}){const [text,setText]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const submit=async e=>{e.preventDefault();if(!text.trim())return;setBusy(true);setError('');try{await sendMessage(conversationId,text.trim());setText('');onSent()}catch(err){setError(err.message)}finally{setBusy(false)}};return <form onSubmit={submit}><label htmlFor="message">Mensaje</label><textarea id="message" value={text} onChange={e=>setText(e.target.value)} maxLength={5000}/>{error&&<p role="alert">{error}</p>}<Button type="submit" loading={busy}>Enviar</Button></form>}
