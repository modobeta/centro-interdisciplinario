import Modal from '../../../components/modals/Modal';import StatusBadge from '../../../components/ui/StatusBadge';import { formatDateTime } from '../../../utils/formatters'
export default function AppointmentDetailModal({appointment,onClose}){return <Modal open={Boolean(appointment)} title="Detalle del turno" onClose={onClose}>{appointment&&<dl><dt>Paciente</dt><dd>{appointment.paciente?.nombreCompleto}</dd><dt>Horario</dt><dd>{formatDateTime(appointment.inicioAt)}</dd><dt>Servicio</dt><dd>{appointment.servicio?.nombre}</dd><dt>Prestador</dt><dd>{appointment.prestador?.nombreCompleto}</dd><dt>Consultorio</dt><dd>{appointment.consultorio?.nombre}</dd><dt>Estado</dt><dd><StatusBadge status={appointment.estado}/></dd></dl>}</Modal>}
