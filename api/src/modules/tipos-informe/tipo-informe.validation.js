const Joi = require('joi'); const id = Joi.string().uuid().required(); const body = Joi.object({ nombre: Joi.string().trim().max(150).required(), descripcion: Joi.string().trim().allow(null) }); module.exports = { list: { query: Joi.object({ page: Joi.number().integer().min(1).default(1), limit: Joi.number().integer().min(1).max(100).default(20), search: Joi.string().trim().max(150), activo: Joi.boolean().default(true), sort: Joi.string().valid('nombre', 'createdAt', 'updatedAt').default('nombre'), order: Joi.string().lowercase().valid('asc', 'desc').default('asc') }) }, id: { params: Joi.object({ id }) }, create: { body }, update: { params: Joi.object({ id }), body }, state: { params: Joi.object({ id }), body: Joi.object({ activo: Joi.boolean().required() }) } };
