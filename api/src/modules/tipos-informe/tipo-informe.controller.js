/** Adapta el catálogo de tipos de informe al formato uniforme de la API. */
const s = require('./tipo-informe.service'); module.exports = { list: async (req, res) => res.json(await s.list(req.actor, req.query)), get: async (req, res) => res.json({ data: await s.get(req.actor, req.params.id) }), create: async (req, res) => res.status(201).json({ data: await s.create(req.actor, req.body, req) }), update: async (req, res) => res.json({ data: await s.update(req.actor, req.params.id, req.body, req) }), state: async (req, res) => res.json({ data: await s.changeState(req.actor, req.params.id, req.body.activo, req) }) };
