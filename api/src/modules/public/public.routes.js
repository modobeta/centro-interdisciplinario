/** Expone sólo equipo y servicios publicados, con límites aun cuando no hay autenticación. */
const express = require('express'); const Joi = require('joi'); const asyncHandler = require('../../shared/utils/asyncHandler'); const validate = require('../../shared/middlewares/validate'); const controller = require('./public.controller'); const router = express.Router(); const query = { query: Joi.object({ limit: Joi.number().integer().min(1).max(50) }) }; router.get('/equipo', validate(query), asyncHandler(controller.team)); router.get('/servicios', validate(query), asyncHandler(controller.services)); module.exports = router;
